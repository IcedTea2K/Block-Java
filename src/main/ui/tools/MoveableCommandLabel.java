package ui.tools;

import model.*;

// Movable command label
public class MoveableCommandLabel extends CommandLabel {
    private Command command;

    public MoveableCommandLabel(CommandType commandType) {
        super(commandType);
    }
}
